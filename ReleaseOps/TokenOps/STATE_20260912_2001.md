# THF TokenOps State — 2026-09-12 20:01 Cairo

Scope: canonical Solana Mainnet mint `HjCHpu3tLRGCkJtZyUjzCKHv47usxWcMcqwhkaeBpjiv` only. WAVE_MAWJA remains out of scope and untouched.

## Pre-change verification

- Branch head inspected: `647f908e1fbfe7e258d4810c033776e0b293baa5` (`feat(tokenops): add replay rollback guard`).
- Authoritative `policy.json` reconfirmed read-only monitoring, 35% active-user revenue share, approved 8B supply-floor target, immutable-disabled minting assumption, and no signing/broadcast/burn/treasury-transfer/authority-change execution while production signer policy is unapproved.
- TokenOps Offline Gates run `34703951021` on the same head completed successfully.
- Token Read-Only Audit run `34703951063` on the same head completed successfully.
- Its `THF-Token-ReadOnly-Audit` artifact ID is `10301461895`, unexpired, with GitHub digest `sha256:66ef5e317de4e3785e178650afcf97dfc904a25008e77b7c1e9e8f316597ae8d`.
- Replay/Rollback Guard remains review-only and requires exact prior-ledger-head continuity; no production treasury address has been inferred or registered.

## Safe checkpoint

Added `external_ledger_anchor_gate.py`, fail-closed tests, and a dedicated CI gate.

The new layer closes the remaining local rollback boundary without pretending Git history itself is an immutable external anchor:

- prepares a canonical anchor request only from a non-empty, verified append-only TokenOps audit ledger;
- binds exact ledger head SHA-256, ledger bytes SHA-256, canonical mint/network, and exact Git source head;
- requires an independently produced `gcp-object-lock` receipt;
- requires short-lived identity (`workload-identity-federation-or-equivalent-short-lived`);
- explicitly rejects persistent service-account-key use;
- requires immutable retention and external verification flags;
- rejects mismatched provider/object/head/ledger/source metadata, malformed timestamps/digests, placeholder object IDs, tampering, and secret/signature fields;
- even a verified external anchor opens `simulation_review_eligible=true` only; it never permits simulation execution, transaction construction, signing, submission, broadcast, transfer, burn, authority change, treasury migration, vesting/reward settlement, or DAO execution.

No production anchor receipt is fabricated in this checkpoint. The gate therefore remains fail-closed until a real external immutable receipt exists.

## Safety invariants

- No seed phrase/private key requested, stored, printed, uploaded, or used.
- No persistent service-account key created or requested.
- No executable transaction created or serialized.
- No transaction signed, submitted, or broadcast.
- No transfer, burn, authority change, treasury migration, vesting settlement, reward settlement, or DAO execution.
- No financial effect.
- Production treasury registry remains intentionally empty pending independently verified owner + exact THF SPL token account + control-evidence digest.
- WAVE_MAWJA untouched.

## Remaining signer action

None.

## Next independent safe work

After CI validates this checkpoint, bind a real WIF-backed immutable-anchor receipt into the replay/freshness review chain when an approved GCP Object Lock destination exists. Until then, continue read-only Mainnet monitoring and independent offline controls without weakening the fail-closed boundary.
