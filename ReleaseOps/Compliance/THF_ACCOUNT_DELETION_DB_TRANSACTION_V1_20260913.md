# THF Account Deletion DB Transaction V1

Status: **PASS-CANDIDATE / DISPOSABLE DB ONLY**

## Evidence
- Successful GitHub Actions run: `34720868082`
- Successful workflow commit: `f01b84cec1db7b549226f01e2f5711f31b31f20a`
- GitHub OIDC/WIF -> GCP -> IAP: PASS
- THF runtime release marker: `6.0.0`
- Deterministic source SHA-256 before/after, excluding generated Python bytecode: `c9244790b9fd688325ce064abdab0f4d91d11c24a5dca905f3e8d9a292cfd740`
- THF/WAVE isolation: PASS

## Tooling correction
An earlier run observed a transient source hash because the old hash recipe included `src/**/__pycache__/*.pyc`. Importing THF generated bytecode and changed that aggregate hash without changing source. The gate now excludes `__pycache__`, `*.pyc`, and `*.pyo` and compares the deterministic source hash before/after.

## Deletion transaction result
The disposable SQLite test created one synthetic user, a mandatory `chat_messages` row, and a retained-attribution `domain_events` row. The metadata-driven transaction processed **26 direct foreign-key references** to `users`:
- `CASCADE` / `SET NULL`: defer to schema behavior;
- mandatory `NO ACTION` rows, including `user_id` participating in a primary key: delete user-owned rows;
- nullable attribution rows: anonymize by setting `user_id=NULL`.

Post-transaction checks:
- users = 0
- sessions = 0
- profiles = 0
- farm_state = 0
- chat_messages = 0
- retained domain-event user attribution = 0
- `PRAGMA foreign_key_check` errors = 0

## Safety
- Production DB mutation: FALSE
- Canonical source mutation: FALSE
- Production signing: FALSE
- Google Play upload: FALSE
- Cloudflare production cutover: FALSE
- Solana financial action: FALSE
- WAVE_MAWJA source/runtime mutation: FALSE

## Remaining account-deletion work
This gate proves the database transaction strategy only. Release readiness still requires:
1. authenticated self-service endpoint whose target identity comes exclusively from the validated session;
2. versioned-source implementation and parser/API tests on a disposable candidate;
3. user-owned media/avatar/object cleanup policy and tests;
4. independent public `/account-deletion` instructions/page;
5. privacy/terms/security.txt resources and final Play Data Safety consistency review.

Next: inspect the existing request/auth routing contract and implement the endpoint only in a reversible versioned/temp candidate, never the production runtime.