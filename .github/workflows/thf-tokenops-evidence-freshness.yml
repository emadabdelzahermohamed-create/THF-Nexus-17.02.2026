name: THF TokenOps Evidence Freshness Gate

on:
  push:
    branches:
      - 'tokenops/**'
    paths:
      - 'ReleaseOps/TokenOps/evidence_freshness_gate.py'
      - 'ReleaseOps/TokenOps/test_evidence_freshness_gate.py'
      - '.github/workflows/thf-tokenops-evidence-freshness.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  evidence-freshness-gate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@692973e3d937129bcbf40652eb9f2f61becf3332

      - name: Run fail-closed freshness tests
        shell: bash
        run: |
          set -Eeuo pipefail
          cd ReleaseOps/TokenOps
          python3 -m unittest -v test_evidence_freshness_gate.py 2>&1 | tee evidence-freshness-tests.log

      - name: Assert non-executing boundary
        shell: bash
        run: |
          set -Eeuo pipefail
          grep -q '^OK$' ReleaseOps/TokenOps/evidence-freshness-tests.log
          ! grep -R -nE 'sendTransaction|send_transaction|broadcast_transaction|Keypair\.from|seed phrase|BEGIN (RSA|OPENSSH|EC) PRIVATE KEY' \
            ReleaseOps/TokenOps/evidence_freshness_gate.py \
            ReleaseOps/TokenOps/test_evidence_freshness_gate.py
          echo THF_TOKENOPS_EVIDENCE_FRESHNESS_GATE=PASS
          echo READONLY_MAX_AGE_SECONDS=7200
          echo CONTROL_MAX_AGE_SECONDS=86400
          echo SIMULATION_REVIEW_ONLY=TRUE
          echo SIMULATION_EXECUTION_PERMITTED=FALSE
          echo TRANSACTION_CREATED=FALSE
          echo TRANSACTION_SIGNED=FALSE
          echo TRANSACTION_SUBMITTED=FALSE
          echo BROADCAST_ALLOWED=FALSE
          echo PRIVATE_KEY_USED=FALSE
          echo FINANCIAL_EFFECT=FALSE
          echo WAVE_UNTOUCHED=TRUE

      - name: Produce evidence hashes
        shell: bash
        run: |
          set -Eeuo pipefail
          mkdir -p tokenops-evidence-freshness
          sha256sum \
            ReleaseOps/TokenOps/evidence_freshness_gate.py \
            ReleaseOps/TokenOps/test_evidence_freshness_gate.py \
            .github/workflows/thf-tokenops-evidence-freshness.yml \
            > tokenops-evidence-freshness/SHA256SUMS.txt
          cp ReleaseOps/TokenOps/evidence-freshness-tests.log tokenops-evidence-freshness/
          printf '%s\n' \
            'THF_TOKENOPS_EVIDENCE_FRESHNESS_GATE=PASS' \
            'READONLY_MAX_AGE_SECONDS=7200' \
            'CONTROL_MAX_AGE_SECONDS=86400' \
            'SIMULATION_REVIEW_ONLY=TRUE' \
            'SIMULATION_EXECUTION_PERMITTED=FALSE' \
            'TRANSACTION_CREATED=FALSE' \
            'TRANSACTION_SIGNED=FALSE' \
            'TRANSACTION_SUBMITTED=FALSE' \
            'BROADCAST_ALLOWED=FALSE' \
            'PRIVATE_KEY_USED=FALSE' \
            'FINANCIAL_EFFECT=FALSE' \
            'WAVE_UNTOUCHED=TRUE' \
            > tokenops-evidence-freshness/SAFETY.txt

      - name: Upload gate evidence
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: THF-TokenOps-Evidence-Freshness-Gate
          path: tokenops-evidence-freshness/
          if-no-files-found: error
          retention-days: 90
