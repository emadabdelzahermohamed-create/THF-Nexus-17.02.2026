name: RuinsCiv RC35 Integrated Build

on:
  push:
    branches:
      - 't/**'
      - 'releaseops/**'
    paths:
      - '.github/workflows/terra-social-modern-build.yml'
      - 'ReleaseOps/scripts/build_terra_rc35_social_phone.sh'
      - 'ReleaseOps/scripts/patch_ruinsciv_identity_v1.py'
      - 'ReleaseOps/scripts/verify_terra_modern_human_only.sh'
      - 'ReleaseOps/scripts/verify_terra_uploaded_asset_lineage.sh'
      - 'ReleaseOps/scripts/patch_terra_modern_human_current_source.py'
      - 'ReleaseOps/scripts/patch_terra_social_auth_v1.py'
      - 'ReleaseOps/scripts/patch_terra_account_deletion_v1.py'
      - 'ReleaseOps/Terra/social_auth_v1/**'
      - 'ReleaseOps/Terra/account_deletion_v1/**'
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

env:
  GCP_PROJECT_ID: project-b5e10d7e-8ce8-4aa4-a15
  GCP_ZONE: europe-west1-b
  GCP_BUILDER: thf-wave-builder
  GCP_WIF_PROVIDER: projects/1035420130588/locations/global/workloadIdentityPools/thf-github-pool/providers/thf-github-provider
  GCP_SERVICE_ACCOUNT: thf-release-builder@project-b5e10d7e-8ce8-4aa4-a15.iam.gserviceaccount.com

jobs:
  build-ruinsciv:
    runs-on: ubuntu-latest
    timeout-minutes: 50
    steps:
      - name: Checkout exact branch
        uses: actions/checkout@692973e3d937129bcbf40652eb9f2f61becf3332

      - id: auth
        name: Authenticate with existing WIF
        uses: google-github-actions/auth@f112390a2df9932162083945e46d439060d66ec2
        with:
          workload_identity_provider: ${{ env.GCP_WIF_PROVIDER }}
          service_account: ${{ env.GCP_SERVICE_ACCOUNT }}
          create_credentials_file: true
          export_environment_variables: true

      - name: Install Google Cloud CLI
        uses: google-github-actions/setup-gcloud@e427ad8a34f8676edf47cf7d7925499adf3eb74f
        with:
          project_id: ${{ env.GCP_PROJECT_ID }}

      - name: Prepare isolated RuinsCiv execution directory
        shell: bash
        run: |
          set -Eeuo pipefail
          REMOTE_OPS="/tmp/terra-social-ops-${GITHUB_RUN_ID}"
          REMOTE_BUILD="/tmp/terra-social-build-${GITHUB_RUN_ID}"
          echo "REMOTE_OPS=$REMOTE_OPS" >> "$GITHUB_ENV"
          echo "REMOTE_BUILD=$REMOTE_BUILD" >> "$GITHUB_ENV"
          gcloud config set project "$GCP_PROJECT_ID" --quiet
          gcloud compute ssh "$GCP_BUILDER" \
            --project "$GCP_PROJECT_ID" \
            --zone "$GCP_ZONE" \
            --tunnel-through-iap \
            --ssh-key-expire-after=10m \
            --quiet \
            --command="set -Eeuo pipefail; test -d \"\$HOME/thf-terra-rift-staging-apk-v1/terra/work\"; rm -rf '$REMOTE_OPS' '$REMOTE_BUILD'; mkdir -p '$REMOTE_OPS/overlay/app' '$REMOTE_OPS/delete-overlay/web/templates'; echo TERRA_SOURCE_PRESENT=PASS; echo WAVE_UNTOUCHED=TRUE"

      - name: Copy RuinsCiv RC35 integration only
        shell: bash
        run: |
          set -Eeuo pipefail
          gcloud compute scp \
            ReleaseOps/scripts/build_terra_rc35_social_phone.sh \
            ReleaseOps/scripts/patch_ruinsciv_identity_v1.py \
            ReleaseOps/scripts/verify_terra_modern_human_only.sh \
            ReleaseOps/scripts/verify_terra_uploaded_asset_lineage.sh \
            ReleaseOps/scripts/patch_terra_modern_human_current_source.py \
            ReleaseOps/scripts/patch_terra_social_auth_v1.py \
            ReleaseOps/scripts/patch_terra_account_deletion_v1.py \
            "$GCP_BUILDER:$REMOTE_OPS/" \
            --project "$GCP_PROJECT_ID" \
            --zone "$GCP_ZONE" \
            --tunnel-through-iap \
            --quiet
          gcloud compute scp \
            ReleaseOps/Terra/social_auth_v1/app/auth_social.py \
            "$GCP_BUILDER:$REMOTE_OPS/overlay/app/auth_social.py" \
            --project "$GCP_PROJECT_ID" \
            --zone "$GCP_ZONE" \
            --tunnel-through-iap \
            --quiet
          gcloud compute scp \
            ReleaseOps/Terra/account_deletion_v1/web/templates/account_delete.html \
            "$GCP_BUILDER:$REMOTE_OPS/delete-overlay/web/templates/account_delete.html" \
            --project "$GCP_PROJECT_ID" \
            --zone "$GCP_ZONE" \
            --tunnel-through-iap \
            --quiet

      - name: Execute RuinsCiv RC35 exact-source Godot and Android gates
        shell: bash
        run: |
          set -Eeuo pipefail
          gcloud compute ssh "$GCP_BUILDER" \
            --project "$GCP_PROJECT_ID" \
            --zone "$GCP_ZONE" \
            --tunnel-through-iap \
            --ssh-key-expire-after=10m \
            --quiet \
            --command="set -Eeuo pipefail; chmod +x '$REMOTE_OPS'/*.sh '$REMOTE_OPS'/*.py; THF_TERRA_SOURCE=\"\$HOME/thf-terra-rift-staging-apk-v1/terra/work\" THF_TERRA_BUILD_ROOT='$REMOTE_BUILD' THF_TERRA_SOCIAL_AUTH_OVERLAY='$REMOTE_OPS/overlay' THF_TERRA_ACCOUNT_DELETE_OVERLAY='$REMOTE_OPS/delete-overlay' bash '$REMOTE_OPS/build_terra_rc35_social_phone.sh'"

      - name: Pull build evidence and APK
        shell: bash
        run: |
          set -Eeuo pipefail
          mkdir -p terra-artifacts
          gcloud compute scp --recurse \
            "$GCP_BUILDER:$REMOTE_BUILD/out" \
            terra-artifacts/ \
            --project "$GCP_PROJECT_ID" \
            --zone "$GCP_ZONE" \
            --tunnel-through-iap \
            --quiet
          test -s terra-artifacts/out/EVIDENCE.json
          test -s terra-artifacts/out/RuinsCiv-4.7.0-RC35-SOCIAL-MODERN-HUMAN-TEST.apk
          grep -q '"status": "PASS_BUILD_AND_ENGINE_GATES"' terra-artifacts/out/EVIDENCE.json
          grep -q '"uploaded_visual16a_lineage_gate": "PASS"' terra-artifacts/out/EVIDENCE.json
          grep -q '"legacy_package_in_active_runtime": false' terra-artifacts/out/EVIDENCE.json
          grep -q '"legacy_public_brand_in_active_runtime": false' terra-artifacts/out/EVIDENCE.json
          grep -q '"standalone_local_auth": "PATCHED_AND_STATIC_GATED"' terra-artifacts/out/EVIDENCE.json
          grep -q '"account_deletion_api_and_external_web_resource": "PATCHED_AND_STATIC_GATED"' terra-artifacts/out/EVIDENCE.json
          echo WAVE_UNTOUCHED=TRUE
          cat terra-artifacts/out/EVIDENCE.json

      - name: Upload RuinsCiv RC35 build artifact
        uses: actions/upload-artifact@v4
        with:
          name: RuinsCiv-RC35-Social-Modern-Human-Test-${{ github.run_id }}
          retention-days: 14
          path: terra-artifacts/out/
          if-no-files-found: error

      - name: Cleanup isolated remote run directory
        if: always()
        shell: bash
        run: |
          set +e
          gcloud compute ssh "$GCP_BUILDER" \
            --project "$GCP_PROJECT_ID" \
            --zone "$GCP_ZONE" \
            --tunnel-through-iap \
            --quiet \
            --command="rm -rf '$REMOTE_OPS' '$REMOTE_BUILD'" >/dev/null 2>&1
          exit 0
